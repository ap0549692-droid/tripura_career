[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "php82Extensions.gd", "php82Extensions.curl", "php82Extensions.mbstring", "php82Extensions.zip", "php82Extensions.pdo_mysql", "php82Extensions.bcmath"]

[phases.install]
cmds = ["composer install --no-dev --no-interaction --no-scripts --ignore-platform-reqs"]

[phases.build]
cmds = []

[start]
cmd = "php artisan migrate --force || true && php artisan serve --host=0.0.0.0 --port=$PORT"
